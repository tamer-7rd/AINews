# ESLint: падаем на любом предупреждении
pnpm exec eslint . --ext .js,.jsx,.ts,.tsx --max-warnings=0 && echo "ESLint passed"

# Prettier: проверяем формат (без изменений)
pnpm exec prettier --check .
